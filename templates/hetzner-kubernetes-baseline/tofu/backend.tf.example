# The locked baseline declares a local backend for disposable certification.
# Production operators should publish and lock a derivative template that
# replaces that declaration with a capability-tested remote backend. Backend
# type is OpenTofu configuration and cannot be selected by -backend-config.

path = "/absolute/protected/path/terraform.tfstate"
