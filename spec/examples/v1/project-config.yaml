# Optional committed <deployment-root>/ainfra.config.yaml.
# Repository-controlled project policy is limited to this safe subset.
apiVersion: ainfra.projectious.work/v1
kind: CLIConfig
ui:
  outputStyle: auto
  color: auto
logging:
  level: warn
