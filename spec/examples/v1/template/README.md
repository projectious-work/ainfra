# Example template

This is a structural specification fixture, not a provider implementation and
not evidence of live infrastructure support. It demonstrates the required
manifest, native OpenTofu variables/output, and native Ansible project layout.

See [the standard variable reference](docs/variables.md) for its documented
deployment-facing inputs.
