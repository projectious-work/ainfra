apiVersion: ainfra.projectious.work/v1
kind: Deployment
metadata:
  name: example
  description: Provider-free deployment of the conforming template example
spec:
  template:
    source: local:../template-example
  inputs:
    tofu:
      variableFiles:
        - terraform.tfvars
      backendConfigFiles: []
    ansible:
      variableFiles:
        - ansible-vars.yaml
